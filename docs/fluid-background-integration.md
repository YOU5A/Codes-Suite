# Fluid Background Integration

> Codes Suite v1.3.0 ? ????????????

---

## ??

???????????????? Canvas 2D ??????????

- 7 ???????? / ?? / ?? / ?? / ??? / ?? / ????
- ?????????`colorMode: "auto"`?
- ????????????`colorMode: "cover"`?
- ???????`colorMode: "dynamic"`?
- 30/60 FPS ????
- ????????
- ???? localStorage ???

---

## ????

### ????

| ?? | ?? |
|---|---|
| `src/components/FluidBackground/index.tsx` | React ????? Canvas ??? FluidRenderer ???? |
| `src/components/FluidBackground/renderer.ts` | Canvas 2D ??????????FPS ???GPU ?? |
| `src/components/FluidBackground/config.ts` | ??????????????localStorage ?? |
| `src/components/FluidBackground/presets.ts` | 7 ?????????????????? |
| `src/components/FluidSettingsPanel.tsx` | ???? UI?GlassModal???????? |
| `src/utils/colorExtractor.ts` | ?????????+??????HSL ????? |

### ????

| ?? | ???? |
|---|---|
| `src/pages/MusicManager.tsx` | ?? FluidBackground ?????????????????FPS ?? |

---

## ??

```
MusicManager.tsx
  ??? fluidSettings state (FluidSettingsValues)
  ?     ??? loadFluidSettings() ? localStorage ??
  ?     ??? saveFluidSettings() ? localStorage ?? (useEffect)
  ?     ??? FluidSettingsPanel (GlassModal ????)
  ?
  ??? coverColor state (RGB | null)
  ?     ??? extractDominantColorAsync(coverB64) ? ???
  ?
  ??? <FluidBackground>
        ??? preset, intensity, speedMultiplier, blurAmount
        ??? quality (? fps ??), targetFps
        ??? colorMode, coverColor
        ??? FluidRenderer (Canvas 2D)
              ??? FPS ?? (targetFps ? frameInterval)
              ??? ????? (visibilitychange)
              ??? ????? (setCoverColor)
              ??? ???? (createRadialGradient ? N)
```

---

## ????????

### ??????????

1. ???????
2. ????????? ? ?? "??????" ??
3. ?????
   - **????** ? ???
   - **??** ? ???? (0.1x ~ 3.0x)
   - **??** ? ??/??/??/??/???/??/??
   - **??** ? 30 FPS (??) / 60 FPS (??)
   - **??** ? ?????? (5% ~ 100%)
   - **??** ? ?????? (0 ~ 100%)
   - **????** ? ??/????/????
4. ????????? `localStorage`??????

### ????????

```typescript
// ?????? (presets.ts)
export const PRESETS: Record<FluidPresetId, FluidPreset> = {
  custom: {
    id: "custom",
    palette: {
      colors: [[255, 100, 100], [100, 100, 255], ...], // ?? 5 ?
      background: [10, 10, 30],                          // ???? RGB
    },
    blobCount: 14,        // ???? (?? 8-24)
    blobRadius: [0.2, 0.45],  // ?????? (????)
    blobOpacity: [0.35, 0.6], // ??????
    flowSpeed: 0.5,       // ????
    driftInterval: 4000,  // ???????? (ms)
    jitter: 0.3,          // ????
  },
};

// ????????? (presets.ts)
const THEME_PRESET_MAP: Record<string, FluidPresetId> = {
  light: "aurora",
  dark: "nebula",
  // ...
};

// ?????? (config.ts)
export const DEFAULT_CONFIG: FluidConfig = {
  preset: "auto",
  intensity: 0.6,
  quality: "medium",
  // ...
};
```

### ??????? API

```typescript
// ?? renderer ????????
renderer.setTargetFps(30);           // ????
renderer.setCoverColor([255, 0, 0]); // ??????
renderer.setPreset("aurora");        // ????
renderer.setVisible(false);          // ????
renderer.splat(0.5, 0.5, 1.0);      // ??????
```

---

## ??????

### 1. ???????

? `src/components/FluidBackground/presets.ts` ??

```typescript
// 1. ? FluidPresetId ?????? ID
export type FluidPresetId =
  | "aurora" | "ocean" | "ember" | "nebula"
  | "plasma" | "forest" | "custom"
  | "sunset";  // ??

// 2. ????
export const PRESETS: Record<FluidPresetId, FluidPreset> = {
  // ...existing...
  sunset: {
    id: "sunset",
    name: { zh: "??", en: "Sunset" },
    palette: {
      colors: [
        [255, 140, 60],
        [240, 100, 80],
        [255, 200, 100],
        [220, 80, 40],
        [255, 160, 90],
      ],
      background: [20, 8, 4],
    },
    blobCount: 16,
    blobRadius: [0.20, 0.45],
    blobOpacity: [0.35, 0.65],
    flowSpeed: 0.55,
    driftInterval: 4000,
    jitter: 0.3,
  },
};

// 3. ? FluidSettingsPanel.tsx ? STYLE_OPTIONS ?????
{ id: "sunset", label: { zh: "??", en: "Sunset" } },
```

### 2. ????????

```typescript
// ? renderer.ts ???
setAudioData(frequencyData: Uint8Array): void {
  // ???????? blob ??/???
  for (let i = 0; i < this.blobs.length; i++) {
    const band = frequencyData[i % frequencyData.length] / 255;
    this.blobs[i].opacity = lerp(0.2, 0.8, band);
  }
}
```

### 3. ??? WebGL/WebGPU

?? Canvas 2D ??? 60FPS 14 ???????????? 50+ ??? 120FPS???? `renderer.ts` ? WebGL ???

- ?? `webgl` context + fragment shader ?? metaball
- ???? ? uniform array ?? GPU
- `requestAnimationFrame` ???????

### 4. ?????

`FluidBackground` ????????????????

```tsx
// Dashboard.tsx
<FluidBackground preset="nebula" intensity={0.3} interactive={false} />
```

### 5. ????????

? `FluidConfig.colorMode` ???????? `renderer.ts` ? `render()` ????

```typescript
// "gradient" ? ?????????
// "bpm" ? ???? BPM ??
// "weather" ? ???? API ????
```

---

## localStorage ????

```json
{
  "fluidSettings": {
    "enabled": true,
    "speedMultiplier": 1.0,
    "style": "auto",
    "fps": 60,
    "intensity": 0.6,
    "blurAmount": 0,
    "colorMode": "auto"
  }
}
```

---

## ????

| ?? | 30 FPS | 60 FPS |
|---|---|---|
| rAF ???? | ~30 Hz | ~60 Hz |
| ?????? | ~420/? (14?30) | ~840/? (14?60) |
| ???? | 0 Hz (??) | 0 Hz (??) |
| ???? | ?? (???) | ?? (???) |

---

## ????

| ?? | ?? | ?? |
|---|---|---|
| 1.0 | 2026-07 | ?????7 ??? + Canvas 2D ?? |
| 1.1 | 2026-07 | ???????localStorage ??? |
| 1.2 | 2026-07 | ????????HSL ??????colorMode="cover" |
| 1.3 | 2026-07 | FPS ??????????GPU ?? |
